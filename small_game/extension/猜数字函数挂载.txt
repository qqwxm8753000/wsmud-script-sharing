<hig>猜数字函数挂载</hig><script>var MUDGuess=(function(){var K='mud_channel_data';function gs(){try{return JSON.parse(localStorage.getItem(K))||{};}catch(e){return{};}}return function(tgt){var d=gs(),names=Object.keys(d);if(!names.length){typeof SendCommand==='function'?SendCommand('tm 暂无数据'):alert('暂无数据');return;}if(tgt===undefined||tgt===null)tgt=Math.floor(Math.random()*100)+1;var r=names.map(function(n){return{name:n,val:parseInt(d[n])||0,diff:Math.abs((parseInt(d[n])||0)-tgt)};}).sort(function(a,b){return a.diff-b.diff});var lines=[],hdr='tm 🎯目标:'+tgt+' 比大小结果：',ents=r.map(function(p,i){var tag=i===0?'🥇':i===1?'🥈':i===2?'🥉':(i+1)+'·';return tag+p.name+'('+p.val+')差'+p.diff});if(ents.length<=5){lines.push(hdr+' '+ents.join(' '));}else{var chunks=[];for(var i=0;i<ents.length;i+=5)chunks.push(ents.slice(i,i+5));chunks.forEach(function(c,idx){lines.push(idx===0?hdr+' '+c.join(' '):'tm '+c.join(' '));});}lines.push('tm 共 '+r.length+' 人参与');lines.forEach(function(l){typeof SendCommand==='function'?SendCommand(l):alert(l);});};})();inteam=[];</script>